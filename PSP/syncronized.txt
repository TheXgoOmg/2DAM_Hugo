synchronized (lockObject) {
                        try (FileOutputStream fos = new FileOutputStream(resultados, true)) {
                            fos.write(bytesResultado);
                        }
                    }
